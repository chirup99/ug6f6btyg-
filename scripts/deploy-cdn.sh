#!/bin/bash
# ⚠️  This script uses raw S3 URLs — no edge caching.
# Use scripts/deploy-cloudfront.sh instead for full CloudFront CDN deploy.
echo "⚠️  Use scripts/deploy-cloudfront.sh for full CloudFront CDN deployment."
echo "   Run setup first (once): npx tsx scripts/setup-cloudfront.ts"
echo "   Then deploy:            bash scripts/deploy-cloudfront.sh"
